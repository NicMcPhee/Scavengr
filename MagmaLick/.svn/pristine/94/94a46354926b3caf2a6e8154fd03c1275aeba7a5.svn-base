package scavengr

import grails.test.mixin.TestFor
//import scavengr.Photo
import spock.lang.Specification

/**
 * See the API for {@link grails.test.mixin.domain.DomainClassUnitTestMixin} for usage instructions
 */
@TestFor(Photo)
class PhotoSpec extends Specification {

	@Override
	def setup() {
	}

	@Override
	def cleanup() {
	}
/*
	def "testPhotoConstraints"() {
		when:
		byte[] testPhoto = [10101010]
		def photo1 = new Photo(title: 'testPhoto1', description: 'testPhotoDescription1', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		def photo2 = new Photo(title: 'testPhoto2', description: '', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		def photo3 = new Photo(title: '', description: 'testPhotoDescription3', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		def photo4 = new Photo(title: '', description: '', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		mockForConstraintsTests(Photo, [photo1, photo2, photo3, photo4])
		
		then:
		photo1.validate()
		photo2.validate()
		photo3.validate()
		photo4.validate()
	}
	
	def "testToString"() {
		when:
		byte[] testPhoto = [10101010]
		def photo5 = new Photo(title: 'testPhoto5', description: 'testPhotoDescription5', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		def photo6 = new Photo(title: 'testPhoto6', description: '', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		def photo7 = new Photo(title: '', description: 'testPhotoDescription7', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		def photo8 = new Photo(title: '', description: '', 
			myUser: new User(), myPrompt: new Prompt(), myFile: testPhoto)
		
		then:
		photo5.toString() == 'testPhoto5'
		photo6.toString() == 'testPhoto6'
		photo7.toString() == ''
		photo8.toString() == ''
	}
	*/
}